@model IEnumerable<mvc_app.Models.User>
@{
    ViewBag.Title = "Admin site";
}
<a asp-action="Create">Добавить пользователя</a>

<table class="table table-stripped">
    <thead>
        <tr>
            <th>Имя</th>
            <th>Роль</th>
            <th style="width:10%">Действия</th>
        </tr>
    </thead>
    <tbody>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.Number</td>
            <td>@item.RoleId</td>
            <td>
                <!--<div class="btn-group">
                    <button type="button" class="btn btn-default dropdown-toggle" aria-haspopup="true" aria-expanded="false">
                        Действия
                    </button>
                    <div class="dropdown-menu shadow animated--grow-in">
                        <a style='margin-right:16px' asp-controller="Admin" asp-action="Edit" asp-route-id="@item.Id" class="dropdown-item">
                            <i class="fas fa-wrench"></i> Изменить
                        </a>
                    </div>
                </div>-->
                <a asp-action="Details" asp-controller="Admin" asp-route-id="@item.Id">Подробнее</a> |
                <a asp-action="Edit" asp-controller="Admin" asp-route-id="@item.Id">Изменить</a> |
                <a asp-action="Delete" asp-controller="Admin" asp-route-id="@item.Id">Удалить</a>
            </td>
        </tr>
    }
    </tbody>
</table>